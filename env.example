# Database Configuration
# For Neon Database, get this from your Neon dashboard
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Optional: For local development with a specific database
# Uncomment and configure if you want to use a local PostgreSQL instance
# DATABASE_URL=postgresql://localhost:5432/toss_up_dev

# Plausible Analytics Configuration
# Set this to your domain for production (e.g., 'yourdomain.com')
# For local development, either:
# 1. Leave unset (analytics disabled locally)
# 2. Set to a custom local domain like 'toss-up.local' and add to /etc/hosts
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=yourdomain.com
